import type { Recommendation } from "@/entities/recommendation";

export const mockRecommendation: Recommendation = {
  id: "rec-1",
  title: "Try one focused practice session",
  reason: "Build confidence with a short, repeatable step.",
};
